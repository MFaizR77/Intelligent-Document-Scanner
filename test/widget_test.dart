// Smoke test placeholder. Aplikasi memerlukan kamera + Hive yang
// di-init di main.dart, sehingga tidak praktis untuk test widget biasa.
// File ini sengaja minimal supaya `flutter analyze` tidak gagal.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
